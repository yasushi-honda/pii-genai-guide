---
interface Props {
  num: number;
}

const { num } = Astro.props;
---

<figure class="gp-figure">
  <slot />
  <figcaption class="gp-figure-caption">図{num}. <slot name="caption" /></figcaption>
</figure>
